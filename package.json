{
  "name": "azura-cafe",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter azura dev",
    "build": "pnpm --filter azura build && rm -rf dist && cp -r artifacts/azura/dist dist",
    "preview": "pnpm --filter azura preview"
  },
  "private": true
}
